<% if @place %>
	<h2>Events at <%=h @place.name %></h2>
<% elsif @city %>
	<h2>Events in <%=h @city.name %></h2>
<% end %>

<ol class="feed">
	<%= render :partial => "event", :collection => @events %>
</ol>
