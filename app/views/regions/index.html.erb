<% content_module h(@country.title), { :altbox => true, :box_size => 100 } do %>
    <ul class="simple arrows">
	<%= render :partial => "region", :collection => @regions, :locals => { :country => @country } %>
    </ul>
<% end %>