# Root Rails Schema Bootstrapper.
#
# This file contains all of the schema information
# for our Rails app and it also requires the
# Services schema bootstrapper, so services may
# load up schema data.

require "services/schema.rb"